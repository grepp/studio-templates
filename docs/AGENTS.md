# AI Agent Development Guide

## 1. 목적

이 문서는 비개발자 또는 AI를 활용하여 생성된 코드를  
회사 운영 표준에 맞는 형태로 리팩토링하고,  
운영 가능한 서비스로 전환하기 위한 가이드이다.

AI는 본 문서를 기준으로 다음을 수행해야 한다:

- 코드 구조 정리
- 보안 기준 적용
- 실행 및 배포 환경 구성
- 필수 문서 생성

---

## 2. 기본 원칙

- 기능 추가보다 **운영 가능성 확보를 우선**한다
- 불완전한 코드는 보완하되, 추측이 필요한 경우 명시적으로 기록한다 (TODO)
- 민감정보는 제거하거나 dummy 값으로 대체한다
- 모든 민감정보는 코드에서 분리한다 (실행 환경에 맞는 방식 사용)
- 운영 데이터 사용금지
- **사용자는 개발자가 아니다** — 민감 데이터, 외부 인프라 사용이 감지되면 즉시 에스컬레이션한다 (§4.5)

---

## 3. 기술 표준

### 3.1 실행 환경 (Docker 강제)
- **모든 프로젝트는 Docker 기반으로 실행 가능해야 한다** — 예외 없음
- 서버 애플리케이션: 앱 서버 컨테이너로 빌드
- 정적 파일 프로젝트(HTML/JS/CSS): Nginx 등으로 정적 호스팅하는 컨테이너로 빌드
- `docker build` 및 `docker run` 한 번에 실행 가능하도록 구성한다

### 3.2 운영 환경
- 서버가 있는 프로젝트: **Kubernetes (Helm)** 기준으로 구성한다
- 정적 파일 프로젝트: 컨테이너 배포 후 **Kubernetes (Helm)** 또는 정적 호스팅(S3 / CloudFront) 기준으로 구성한다

### 3.3 데이터베이스
- DB가 필요한 프로젝트는 **PostgreSQL 16.x 기준**으로 구성한다
- DB 연결은 민감정보 분리 파일을 통해 처리한다

### 3.4 언어
- Python 또는 Ruby 기반을 권장한다
- 기타 언어 사용 시 실행 방법을 명확히 문서화한다

---

## 4. 보안 표준

### 4.1 금지사항
- 비밀번호, API Key, 토큰, 인증정보를 코드에 직접 포함하지 않는다
- DB 접속정보를 코드 또는 문서에 평문으로 저장하지 않는다

### 4.2 민감정보 분리

모든 민감정보(API Key, 비밀번호, 토큰, DB 접속정보)는 코드에서 분리한다.
실행 환경에 따라 적절한 방식을 선택한다:

| 환경 | 분리 방식 | 예시 파일 | 실제 파일 (.gitignore 등록) |
|------|-----------|-----------|---------------------------|
| 서버 런타임 (Node.js, Python 등) | 환경 변수 파일 | `.env.example` | `.env` |
| 브라우저 only (HTML/JS) | JS 설정 파일 | `app-config.example.js` | `app-config.js` |
| 브라우저 only (대안) | JSON 설정 파일 | `config.example.json` | `config.json` |
| 빌드 도구 사용 (Vite, webpack 등) | `.env` + 빌드 주입 | `.env.example` | `.env` |

**원칙:**
- 예시 파일(`.example`)은 dummy 값으로 Git에 커밋한다
- 실제 파일은 `.gitignore`에 등록하여 커밋하지 않는다
- 기존 프로젝트가 이미 분리 방식을 사용 중이면 그 방식을 유지한다
- **프로젝트 환경에 맞는 방식 하나만 선택**하여 적용한다. 여러 방식을 중복 생성하지 않는다
  - 예: HTML-only 프로젝트에 `app-config.example.js`를 선택했으면 `.env.example`은 생성하지 않는다

**분리 방식:**
- 소스 코드에서 민감값을 추출하여 **참조(변수/설정 파일 로드)**로 대체한다
- 소스 코드는 참조만 남고 더 이상 실제 값을 포함하지 않는다
- 예시 파일(`.example`)에는 dummy 값(예: `your-api-key-here`)을 넣는다
- **실제 파일에 소스 코드에서 추출한 기존 값을 그대로 넣는다. 절대 삭제하지 않는다.**
- 앱은 실제 파일이 있으면 즉시 동작해야 한다

**사용자 안내 (필수):**
완료 보고 시 사용자에게 다음을 반드시 안내한다:
- 기존에 Git에 커밋된 민감정보(API Key, 비밀번호 등)가 있으면 **해당 값을 즉시 재발급**받아야 한다
- 재발급 후 실제 파일(`.env`, `app-config.js` 등)의 값을 새 값으로 교체해야 한다
- 실제 파일은 `.gitignore`에 등록되어 있으므로 Git에 커밋되지 않는다

### 4.3 인증 및 권한
다음 중 하나에 해당하면 인증 구조를 추가한다:

- 여러 사용자가 접근하는 경우
- 데이터 조회/수정 기능이 있는 경우

가능한 경우:
- 기본 인증 구조 (JWT, Session 등) 추가
- 관리자 / 일반 사용자 구분

### 4.4 로그 보안
- 로그에 다음 정보가 포함되지 않도록 한다:
  - 개인정보
  - 인증 토큰
  - 비밀번호

### 4.5 민감 데이터 및 인프라 에스컬레이션

사용자는 개발자가 아니다. AI Agent가 **능동적으로** 위험 상황을 감지하고 안내해야 한다.

**다음 중 하나라도 해당하면 작업을 중단하고 사용자에게 안내한다:**

- 사용자가 **개인정보**(이름, 연락처, 이메일, 주민번호, 여권번호 등)를 수집·저장·처리하려는 경우
- 사용자가 **회사의 민감 데이터**(매출, 재무 정보, 고객 데이터, 내부 문서 등)를 다루려는 경우
- **grepp 내부 인프라가 아닌 외부 DB, 외부 배포 환경**을 사용하려는 경우
- 외부 API, 클라우드 서비스(AWS, GCP 등)에 데이터를 전송하려는 경우

**에스컬레이션 안내 메시지:**
```
⚠️ 보안 검토가 필요한 사항이 감지되었습니다.

{감지된 내용 요약}

계속 진행하기 전에 아래 채널에 문의해주세요:
👉 https://grepp.slack.com/archives/C02C3QKHEU8

보안팀의 가이드를 받은 후 작업을 계속 진행하겠습니다.
```

**적용 시점:**
- 설치 인터뷰(질문 4번)에서 민감 데이터 사용이 확인된 경우
- 작업 중 사용자의 요청이 민감 데이터 처리과 관련된 경우
- DB 연결, 외부 API 연동, 배포 환경 설정 시 외부 인프라가 감지된 경우
- **세션 전반에 걸쳐 지속적으로 확인** — 사용자가 명시하지 않아도 코드나 요구사항에서 감지되면 즉시 안내

---

## 5. 시스템 구조 표준

AI는 다음 구조를 만족하도록 리팩토링해야 한다:

### 5.1 민감정보 분리
- §4.2의 분리 방식 중 프로젝트 환경에 맞는 것을 적용한다
- 하드코딩된 민감값을 모두 추출하여 분리 파일로 이동한다

### 5.2 로깅
- 기본적인 로그 출력 구조 추가
- 에러 로그는 명확하게 구분

### 5.3 헬스체크
- 서버가 있는 프로젝트: `/health` 또는 `/status` endpoint 추가
- 정적 파일만 있는 프로젝트: 생략 가능

### 5.4 디렉토리 구조 (권장)
/app
/src
/config
/docker
/helm

---

## 6. 필수 산출물

다음 파일이 없으면 반드시 생성해야 한다:

### 6.1 README.md
- 서비스 개요
- 실행 방법
- 민감정보 설정 방법 (§4.2 기준)
- 담당자

### 6.2 ARCHITECTURE.md
- 시스템 구조
- 데이터 흐름
- 외부 연동
- 인증 구조

### 6.3 RUNBOOK.md
- 배포 방법
- 재시작 방법
- 장애 대응 방법

### 6.4 SECURITY.md
- 보안 적용 내용
- 금지사항
- 데이터 처리 정책

### 6.5 민감정보 분리 파일
- §4.2에 명시된 방식 중 프로젝트에 맞는 것을 선택하여 생성
- dummy 값만 포함, 실제 값은 포함하지 않는다

### 6.6 Dockerfile
- 실행 가능한 컨테이너 정의

### 6.7 SPEC.md
- `.grepp-agent/SPEC.md`에 생성
- **모든 프로젝트**에서 필수 생성
- 기존 프로젝트: 코드 분석을 기반으로 에이전트가 직접 작성
- 초기 프로젝트: 사용자 인터뷰 기반으로 작성
- 서비스 개요, 화면 구성, 데이터 구조, 기술 스택 포함
- 사용자의 요구사항을 비개발자가 이해할 수 있는 언어로 정리
- 초기 프로젝트의 경우 **사용자가 명시적으로 구현을 요청하기 전까지 구현하지 않는다**

### 6.8 DESIGN.md
- `.grepp-agent/DESIGN.md`에 **프로그래머스 Design Guideline** 템플릿을 둔다 (템플릿 설치·업데이트 시 원본과 동기화).
- **사용자 대면 UI**(웹·앱 화면, 콘텐츠 사이트 등)가 있는 프로젝트는 해당 문서를 구현 기준으로 삼는다:
  - 시맨틱 토큰(색·타이포·간격·radius·motion 등)을 우선하고, 컴포넌트 가이드에 없는 임의 hex·일회성 간격을 피한다
  - WCAG 2.2 AA, 키보드·포커스 가시성, 대비 등 접근성 요구를 만족시킨다
  - 인터랙티브 컴포넌트는 default·hover·focus-visible·active·disabled·loading·error 등 **명시된 상태**를 구현한다
- **UI가 없는** 순수 API·배치·CLI만인 프로젝트는 파일은 유지하되, 화면 구현 시까지 내용 적용은 생략할 수 있다.

---

## 7. 외부 API / AI 사용 시

### 7.1 데이터 전송
- 외부 API 또는 LLM으로 전송되는 데이터 종류를 명시한다
- 개인정보 또는 민감정보는 전송하지 않도록 처리한다

### 7.2 출력 검증
- AI 결과는 자동으로 업무에 반영하지 않는다
- 검증 또는 확인 단계 필요

---

## 8. 리팩토링 작업 지침

AI는 다음 순서로 작업을 수행한다:

1. 코드 구조 분석
2. 민감정보 제거
3. 민감정보 분리 구조 적용 (§4.2 기준)
4. Dockerfile 생성 (§3.1 강제)
5. 기본 실행 가능 상태 확보
6. 문서 파일 생성
7. 보안 기준 적용
8. Helm 배포 구조 생성
9. 사용자 대면 UI가 있는 경우 `.grepp-agent/DESIGN.md`의 토큰·접근성·컴포넌트 상태 규칙을 반영한다

---

## 9. 불확실한 경우

다음과 같은 경우 명확히 표시한다:

- TODO:
- FIXME:
- ASSUMPTION:

---

## 10. 참조 문서

AI는 필요 시 다음 문서를 추가로 읽고 반영해야 한다:

| 문서 | 용도 | 언제 읽는가 |
|------|------|-------------|
| `.grepp-agent/ARCHITECTURE.md` | 시스템 구조, 데이터 흐름, 인증 구조 | 아키텍처 설계·변경 시 |
| `.grepp-agent/RUNBOOK.md` | 배포, 롤백, 장애 대응 절차 | 배포·운영 작업 시 |
| `.grepp-agent/SECURITY.md` | 보안 정책, 금지사항, 데이터 처리 | 보안 관련 작업 전 |
| `.grepp-agent/SPEC.md` | 서비스 명세, 화면 구성, 데이터 구조 | 구현·기능 변경 시 |
| `.grepp-agent/DESIGN.md` | 프로그래머스 디자인 토큰·접근성·컴포넌트·품질 게이트 | UI·스타일·콘텐츠 화면 구현·리뷰 시 |

---

## 11. 최종 목표

이 저장소는 다음 상태를 만족해야 한다:

- 다른 개발자가 바로 실행 가능
- Docker로 실행 가능
- 기본적인 운영 대응 가능
- 보안 기준을 위반하지 않음

---

## 11.5 SPEC.md 유지 관리

`.grepp-agent/SPEC.md`는 **살아있는 문서**다. 서비스가 변하면 명세서도 함께 변해야 한다.

**다음 상황에서 반드시 SPEC.md를 업데이트한다:**

- 사용자가 새로운 기능을 요청하거나 기존 기능을 변경할 때
- 구현 완료 후 요구사항과 실제 구현이 달라진 부분이 있을 때
- 사용자가 기능을 제거하거나 축소할 때
- 데이터 구조나 화면 구성에 변화가 생길 때

**업데이트 원칙:**

- SPEC.md는 항상 **현재 서비스의 실제 상태**를 반영해야 한다
- 구현 전 요구사항과 구현 후 실제 내용이 다르면, **실제 구현된 내용** 기준으로 갱신한다
- 변경 시 사용자에게 "명세서를 업데이트했습니다"라고 간단히 안내한다
- **절대 과거 요구사항을 그대로 방치하지 마라** — 구현이 바뀌면 SPEC도 바뀌어야 한다

---

## 12. 템플릿 버전 관리

### 세션 시작 시 버전 확인

사용자의 **첫 번째 요청**을 받으면, 본격적인 작업에 앞서 **반드시** 다음 절차를 수행한다:

1. **로컬 버전 확인**:
   ```bash
   cat .grepp-agent/VERSION
   ```
   - 파일이 없으면 템플릿이 설치되지 않은 것이다 → 설치 가이드(`installation.md`)를 안내한다

2. **원격 버전 확인**:
   ```bash
   curl -s https://raw.githubusercontent.com/grepp/studio-templates/refs/heads/main/docs/VERSION
   ```

3. **버전 비교**:
   - 원격 버전 > 로컬 버전 → 업데이트 절차 진행
   - 동일 → 아무 작업 없이 사용자 요청을 처리한다

### 업데이트 절차

새 버전이 감지되면:

1. **업데이트 가이드 fetch**:
   ```bash
   curl -s https://raw.githubusercontent.com/grepp/studio-templates/refs/heads/main/docs/update.md
   ```

2. update.md의 지시에 따라 변경 파일을 비교하고 업데이트를 적용한다

3. 사용자에게 변경 내역을 보고하고 동의를 구한 후 실행한다

4. 업데이트 완료 후 `.grepp-agent/VERSION`을 새 버전으로 갱신한다

---
